# DL package
# Submodules are imported explicitly where needed:
#   data_preparation  — Tab 1 (dataset preparation, no torch required)
#   architectures     — Tab 2/3 (model training/inference, requires torch)
